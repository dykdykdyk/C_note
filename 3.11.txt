do while语句是查看while语句里面的条件是否满足，满足的话 才会执行do里面的语句 
do{
    printf("");
   }
  while(i<=100)
 do while和while语句的区别在于，do while 会至少执行一个do里面的方法；
 break是跳出当前循环体，而continue是结束本次循环
  break语句只能用于循环语句和switch语句中，而不能单独使用

数组不能够动态定义
在字符串数组中，当其内容为‘\0’的时候，表示数组的真实长度为前面数组下标的位置。
char c[] ={"I am happy"};等价于 char c[] ={'I','','a','m','','h','a','p','p','y','\0'};

char strl[5],str2[5],str3[5];
scanf("%s%s%s",str1,str2,str3); 输入 how are you?  由于中间有空格符，他会
以三个字符串依次输入进str1,str2,str3。 //注意书写，数组名代表该数组的起始位置，
所以不用加入&符。

字符串函数： gets(字符数组)(), puts(字符数组)(输出到屏幕),
数组输出的时候，不能输出他的元素名
  字符串连接函数strcat(字符数组1,字符数组2) 注意：字符数组1必须要足够大，在
字符数组1中会有一个保留位。
  strcpy(字符数组1，字符串2)
  strncpy(str1,str2,2)是将字符串2中的前面两个字符复制到str1中。
  strcmp(字符串1，字符串2) 比较两个字符串大小 strcmp(str1,str2).如果相等，函数值为0，如果1大于2，函数值为正整数，
如果1小于2，函数值为一个负整数。
  strlen(字符数组)函数 --测字符串长度的函数，输出的是字符串的真实长度。
  strlwr(字符串)转换成小写的函数
  strupr(字符串)转换成大写函数
  #include<string.h> 字符串函数库

函数
   main函数不能被其他函数调用.
   在main函数中对其他需要调用的函数进行声明.
   实参向形参的数据传递是单向传递，只能由实参传给形参，而不能由形参传给实参。

  
  